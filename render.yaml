services:
  - type: web
    name: golf-tournament
    env: python
    buildCommand: ./build.sh
    startCommand: gunicorn app:socket_app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.18
