services:
  - type: web
    name: golf-tournament
    env: python
    buildCommand: bash build.sh
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.8.18
      - key: NODE_VERSION
        value: 18.x
